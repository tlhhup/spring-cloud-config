server:
  port: 10012
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10010/eureka/
  instance:
    appname: config-server
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tlhhup/config-reps